{
  "name": "contentmanagement-cpanel",
  "private": true,
  "scripts": {
    "start": "node server/index.js",
    "build": "npm --prefix client install && npm --prefix client run build",
    "init-db": "node server/init_db.js"
  },
  "dependencies": {
    "better-sqlite3": "^8.0.0",
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "multer": "^1.4.5-lts.1"
  }
}
