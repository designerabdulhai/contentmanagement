PORT=4000
CORS_ORIGIN=http://localhost:5173
MAX_BODY_SIZE=1mb
# Set this only when initializing/updating the owner account.
# Never commit the real password to the repository.
INIT_ADMIN_PASSWORD=change-me-before-running-init-db
